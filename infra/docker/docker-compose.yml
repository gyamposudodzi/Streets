services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

  backend:
    build:
      context: ../../backend
    ports:
      - "8000:8000"
    volumes:
      - ../../backend/data:/app/data
    depends_on:
      - redis
